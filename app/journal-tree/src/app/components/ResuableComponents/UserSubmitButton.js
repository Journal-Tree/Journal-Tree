const styles = {
  width: 44.15,
  height: 68,
};

const UserSubmitButton = () => (
  <img style={styles} src="./assets/UserSubmitButton.png" alt="Submit Button" />
);

export default UserSubmitButton;
